import styled from 'styled-components';

export const LoaderWrap = styled('div')({
  position: 'absolute',
  top: '0',
  left: '0',
  width: '100%',
  height: '100%',
  background: 'rgba(0, 0, 0 , 0.8)',
  opacity: '1',
  zIndex: '10',
});
